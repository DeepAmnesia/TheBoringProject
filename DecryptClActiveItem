std::uint64_t DecryptClActiveItem(std::uint64_t a1)
{
    std::uint64_t rax = a1;
    std::uint32_t* chunks = (std::uint32_t*)&rax;

    for (int i = 0; i < 2; i++)
    {
        std::uint32_t v = chunks[i];
        v ^= 0x4A187FCEu;
        v += 0x30A95362u;
        chunks[i] = v;
    }

    return rax;
}
