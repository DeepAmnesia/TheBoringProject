std::uint64_t DecryptClActiveItem(std::uint64_t a1)
{
    uint64_t encrypted = a1;
    uint64_t value = encrypted;
    uint32_t* chunks = reinterpret_cast<uint32_t*>(&value);
    
    for (int i = 0; i < 2; i++) 
    {
        uint32_t v = chunks[i];
        v ^= 0xC7F486D7u;
        v += 0x021AB371u;
        v = (v << 11) | (v >> 21);
        v += 0x70077F50u;
        chunks[i] = v;
     }
    
     return value;
}
