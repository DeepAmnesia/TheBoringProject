std::uint64_t DecryptClActiveItem(std::uint64_t a1)
{
    std::uintptr_t val = a1;
    std::uintptr_t r8 = (std::uintptr_t)&val;

    std::uint32_t r8d = 0x2;
    std::uint32_t eax, edx;
    do
    {
        eax = *(std::uint32_t*)(r8);
        r8 = (std::uintptr_t)((std::uint8_t*)r8 + 0x4);
        eax ^= 0x8AE931D7;
        edx = eax;
        edx >>= 0x09;
        edx |= (std::uint32_t)eax << 0x17;
        edx += 0x25B129D5;
        *((std::uint32_t*)r8 - 1) = edx;
        --r8d;
    } while (r8d);

    return val;
}
