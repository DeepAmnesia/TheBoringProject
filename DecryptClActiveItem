std::uint64_t DecryptClActiveItem(std::uint64_t a1)
{
    std::uint64_t value = a1;
    std::uint32_t* chunks = reinterpret_cast<std::uint32_t*>(&value);

    for (int i = 0; i < 2; i++)
    {
        std::uint32_t v = chunks[i];
        v = v + 0xdf0a59;
        v = v ^ 0x22016506;
        v = (v >> 10) | (v << 22);
        v = v - 0x136c7cc5;
        chunks[i] = v;
    }

    return value;
}
